import { CompletionItem } from 'vscode-languageserver';
export declare const dataSourceSuggestions: (currentLine: string, isInsideQuotation: boolean, datasourceProvider: string | undefined) => {
    items: CompletionItem[];
    isIncomplete: boolean;
} | undefined;
