import { Diagnostic } from 'vscode-languageserver';
import { TextDocument } from 'vscode-languageserver-textdocument';
export declare const validateExperimentalFeatures: (document: TextDocument, diagnostics: Diagnostic[]) => void;
export declare const validateIgnoredBlocks: (lines: string[], diagnostics: Diagnostic[]) => void;
