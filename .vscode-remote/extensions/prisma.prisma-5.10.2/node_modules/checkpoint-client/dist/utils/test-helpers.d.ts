/// <reference types="node" />
/// <reference types="node" />
import { ChildProcess } from 'child_process';
import http from 'http';
export declare function serve(handler: http.RequestListener): Promise<Server>;
export type Server = {
    url: string;
    close: () => Promise<void>;
};
type ChildOutput = {
    stdout: string;
    stderr: string;
    code: number | null;
};
export declare function wait(child: ChildProcess): Promise<ChildOutput>;
export declare function waitMs(ms: number): Promise<void>;
export {};
